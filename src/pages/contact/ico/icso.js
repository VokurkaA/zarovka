function icso({ color }) {
    return (
        <div className={`h-8 w-8 bg-primary text-${color} text-sm rounded-full flex items-center justify-center font-bold`}>IČO</div>
    );
}
export default icso;